services:
  api:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
